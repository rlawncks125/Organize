console.log("dadadadwada");

const ds = "ad" as String;

const data: { tuple: [number] } = { tuple: [1] };

const button = document.querySelector("button")!;

button.addEventListener("click", () => {});
